<% include partials/head %>
<% include partials/header %>
<% include partials/container-open %>

<div class="span12">
  <ul>
    <% for(var i in items) { var item = items[i]; %>
      <li><a href="/<%= item.title %>"><%= item.name %></a></li>
    <% } %>
  </ul>
</div>

<% include partials/container-close %>
<% include partials/footer %>